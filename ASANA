Gerard
    - Make sure emails can't be parsed in the HTML body.
    - Allow us to show rich content in a better way
        * Perhaps compile all the links in a post and put them at the bottom.
    - Make it scale on usacs.rutgers.edu

Vaibhav
    - Write an IMAP Wrapper to query rutgersusacs@gmail.com.
